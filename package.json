{
  "scripts": {
    "dev": "npx serve .",
    "build": "rm -rf dist && mkdir -p dist/src dist/assets && cp index.html dist/ && cp src/main.js src/styles.css dist/src/ && cp assets/* dist/assets/",
    "preview": "npx serve dist"
  },
  "dependencies": {},
  "devDependencies": {}
}
